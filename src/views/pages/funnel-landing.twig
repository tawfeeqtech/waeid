Hello Funnel - File is working!

This is a test to confirm the funnel-landing.twig file is properly linked and loading.

{% extends "layouts.master" %}
{% block content %}
<div style="padding: 5rem 0; text-align: center; background: var(--bg-primary);">
    <h1 style="font-size: 3rem; font-weight: 700; color: var(--color-text); font-family: var(--font-main); margin-bottom: 2rem;">
        🎯 Single-Product Digital Funnel is Working!
    </h1>
    <p style="font-size: 1.2rem; color: var(--color-text-secondary); max-width: 600px; margin: 0 auto;">
        All funnel components are now properly linked and should be visible in the preview.
    </p>
    <div style="background: var(--bg-card); border: 1px solid var(--border-color); border-radius: 12px; padding: 2rem; max-width: 400px; margin: 2rem auto;">
        <h3 style="color: var(--color-text); margin-bottom: 1rem;">✅ File Status:</h3>
        <ul style="text-align: left; color: var(--color-text-secondary);">
            <li>📁 funnel-landing.twig exists and is properly formatted</li>
            <li>🎨 funnel.scss is included in app.scss</li>
            <li>📱 JS files are linked in master.twig</li>
            <li>🔄 Production build completed</li>
        </ul>
    </div>
</div>
{% endblock %}
                        <label>
                            <input type="radio" name="level" value="advanced">
                            {{ user.language.code == 'ar' ? 'متقدم - أبحث عن التميز' : 'Advanced - Seeking excellence' }}
                        </label>
                    </div>
                </div>
                
                <div class="form-navigation">
                    <button type="button" class="btn btn--secondary prev-step">
                        {{ user.language.code == 'ar' ? 'السابق' : 'Previous' }}
                    </button>
                    <button type="button" class="btn btn--primary next-step">
                        {{ user.language.code == 'ar' ? 'التالي' : 'Next' }}
                    </button>
                </div>
            </div>
            
            <!-- Step 3: Challenges -->
            <div class="form-step">
                <h3 class="step-title">{{ user.language.code == 'ar' ? 'ما هي أكبر تحدياتك؟' : 'What are your biggest challenges?' }}</h3>
                <p class="step-description">{{ user.language.code == 'ar' ? 'اختر التحديات التي تواجهك حالياً' : 'Choose the challenges you currently face' }}</p>
                
                <div class="form-group">
                    <div class="checkbox-group">
                        <label>
                            <input type="checkbox" name="challenges" value="time">
                            {{ user.language.code == 'ar' ? 'ضيق الوقت' : 'Time constraints' }}
                        </label>
                        <label>
                            <input type="checkbox" name="challenges" value="resources">
                            {{ user.language.code == 'ar' ? 'قلة الموارد' : 'Limited resources' }}
                        </label>
                        <label>
                            <input type="checkbox" name="challenges" value="guidance">
                            {{ user.language.code == 'ar' ? 'نقص التوجيه' : 'Lack of guidance' }}
                        </label>
                        <label>
                            <input type="checkbox" name="challenges" value="motivation">
                            {{ user.language.code == 'ar' ? 'انخفاض الدافعية' : 'Low motivation' }}
                        </label>
                        <label>
                            <input type="checkbox" name="challenges" value="skills">
                            {{ user.language.code == 'ar' ? 'نقص المهارات' : 'Skill gaps' }}
                        </label>
                    </div>
                </div>
                
                <div class="form-navigation">
                    <button type="button" class="btn btn--secondary prev-step">
                        {{ user.language.code == 'ar' ? 'السابق' : 'Previous' }}
                    </button>
                    <button type="button" class="btn btn--primary next-step">
                        {{ user.language.code == 'ar' ? 'التالي' : 'Next' }}
                    </button>
                </div>
            </div>
            
            <!-- Step 4: Contact Info -->
            <div class="form-step">
                <h3 class="step-title">{{ user.language.code == 'ar' ? 'معلومات التواصل' : 'Contact Information' }}</h3>
                <p class="step-description">{{ user.language.code == 'ar' ? 'كيف يمكننا التواصل معك؟' : 'How can we reach you?' }}</p>
                
                <div class="form-group">
                    <label for="name">{{ user.language.code == 'ar' ? 'الاسم الكامل' : 'Full Name' }}</label>
                    <input type="text" id="name" name="name" required>
                </div>
                
                <div class="form-group">
                    <label for="email">{{ user.language.code == 'ar' ? 'البريد الإلكتروني' : 'Email' }}</label>
                    <input type="email" id="email" name="email" required>
                </div>
                
                <div class="form-group">
                    <label for="phone">{{ user.language.code == 'ar' ? 'رقم الهاتف' : 'Phone Number' }}</label>
                    <input type="tel" id="phone" name="phone" required>
                </div>
                
                <div class="form-navigation">
                    <button type="button" class="btn btn--secondary prev-step">
                        {{ user.language.code == 'ar' ? 'السابق' : 'Previous' }}
                    </button>
                    <button type="submit" class="btn btn--primary">
                        {{ user.language.code == 'ar' ? 'إرسال واستمر' : 'Submit & Continue' }}
                    </button>
                </div>
            </div>
        </form>
    </div>
</section>

<!-- Testimonials Section -->
<section class="funnel-testimonials" data-section="testimonials">
    <div class="container">
        <div class="section-header">
            <h2>{{ user.language.code == 'ar' ? 'ماذا يقول عملاؤنا' : 'What Our Clients Say' }}</h2>
            <p class="subtitle">{{ user.language.code == 'ar' ? 'قصص نجاح حقيقية من أشخاص مثلك' : 'Real success stories from people like you' }}</p>
        </div>
        
        <div class="testimonials-grid">
            <div class="testimonial-card">
                <div class="stars">★★★★★</div>
                <p class="testimonial-text">
                    "{{ user.language.code == 'ar' ? 'غيرت حياتي تماماً. بعد 3 شهور فقط، حققت أهدافي التي كنت أحلم بها لسنوات' : 'Completely changed my life. After just 3 months, I achieved goals I had been dreaming of for years' }}"
                </p>
                <div class="author">
                    <img src="https://picsum.photos/seed/user1/50/50.jpg" alt="Author" class="author-avatar">
                    <div class="author-info">
                        <div class="author-name">{{ user.language.code == 'ar' ? 'أحمد محمد' : 'Ahmed Mohammed' }}</div>
                        <div class="author-title">{{ user.language.code == 'ar' ? 'مدير مشروع' : 'Project Manager' }}</div>
                    </div>
                </div>
            </div>
            
            <div class="testimonial-card">
                <div class="stars">★★★★★</div>
                <p class="testimonial-text">
                    "{{ user.language.code == 'ar' ? 'أفضل استثمار قمت به في نفسي. الدعم والمتابعة كانا ممتازين' : 'Best investment I made in myself. The support and follow-up were excellent' }}"
                </p>
                <div class="author">
                    <img src="https://picsum.photos/seed/user2/50/50.jpg" alt="Author" class="author-avatar">
                    <div class="author-info">
                        <div class="author-name">{{ user.language.code == 'ar' ? 'فاطمة العلي' : 'Fatima Al-Ali' }}</div>
                        <div class="author-title">{{ user.language.code == 'ar' ? 'رائدة أعمال' : 'Entrepreneur' }}</div>
                    </div>
                </div>
            </div>
            
            <div class="testimonial-card">
                <div class="stars">★★★★★</div>
                <p class="testimonial-text">
                    "{{ user.language.code == 'ar' ? 'نتائج مذهلة في وقت قياسي. أوصي به بشدة لكل من يبحث عن التميز' : 'Amazing results in record time. Highly recommend to anyone seeking excellence' }}"
                </p>
                <div class="author">
                    <img src="https://picsum.photos/seed/user3/50/50.jpg" alt="Author" class="author-avatar">
                    <div class="author-info">
                        <div class="author-name">{{ user.language.code == 'ar' ? 'خالد السعيد' : 'Khaled Al-Saeed' }}</div>
                        <div class="author-title">{{ user.language.code == 'ar' ? 'مستشار' : 'Consultant' }}</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- FAQ Section -->
<section class="funnel-faq" data-section="faq">
    <div class="container">
        <div class="section-header">
            <h2>{{ user.language.code == 'ar' ? 'أسئلة شائعة' : 'Frequently Asked Questions' }}</h2>
            <p class="subtitle">{{ user.language.code == 'ar' ? 'إجابات على استفساراتك' : 'Answers to your questions' }}</p>
        </div>
        
        <div class="faq-list">
            <div class="faq-item">
                <button class="faq-question">
                    {{ user.language.code == 'ar' ? 'كم من الوقت أحتاج لرؤية النتائج؟' : 'How long until I see results?' }}
                    <span class="icon">▼</span>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        {{ user.language.code == 'ar' ? 'معظم العملاء يبدأون في رؤية نتائج ملحوظة خلال 4-6 أسابيع من البداية' : 'Most clients start seeing noticeable results within 4-6 weeks of starting' }}
                    </div>
                </div>
            </div>
            
            <div class="faq-item">
                <button class="faq-question">
                    {{ user.language.code == 'ar' ? 'هل هناك ضمان استرداد؟' : 'Is there a money-back guarantee?' }}
                    <span class="icon">▼</span>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        {{ user.language.code == 'ar' ? 'نعم، نقدم ضمان استرداد كامل خلال 30 يوماً إذا لم تكن راضياً' : 'Yes, we offer a full money-back guarantee within 30 days if you are not satisfied' }}
                    </div>
                </div>
            </div>
            
            <div class="faq-item">
                <button class="faq-question">
                    {{ user.language.code == 'ar' ? 'هل يمكنني تغيير الباقة لاحقاً؟' : 'Can I change packages later?' }}
                    <span class="icon">▼</span>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        {{ user.language.code == 'ar' ? 'بالتأكيد، يمكنك ترقية أو تخفيض باقتك في أي وقت' : 'Absolutely, you can upgrade or downgrade your package at any time' }}
                    </div>
                </div>
            </div>
            
            <div class="faq-item">
                <button class="faq-question">
                    {{ user.language.code == 'ar' ? 'ما هي وسائل الدفع المتاحة؟' : 'What payment methods are available?' }}
                    <span class="icon">▼</span>
                </button>
                <div class="faq-answer">
                    <div class="answer-content">
                        {{ user.language.code == 'ar' ? 'نقبل جميع البطاقات الائتمانية، Apple Pay، وخدمة الدفع عند الاستلام' : 'We accept all credit cards, Apple Pay, and cash on delivery service' }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Final CTA Section -->
<section style="padding: 5rem 0; background: var(--bg-primary);">
    <div class="container" style="max-width: 800px; margin: 0 auto; padding: 0 2rem; text-align: center;">
        <h2 style="font-size: 2.5rem; font-weight: 700; margin-bottom: 1.5rem; color: var(--color-text); font-family: var(--font-main);">
            {{ user.language.code == 'ar' ? 'هل أنت مستعد لبدء رحلتك؟' : 'Are You Ready to Start Your Journey?' }}
        </h2>
        <p style="font-size: 1.2rem; color: var(--color-text-secondary); margin-bottom: 2.5rem;">
            {{ user.language.code == 'ar' ? 'انضم إلى آلاف العملاء الناجحين وحقق أهدافك اليوم' : 'Join thousands of successful clients and achieve your goals today' }}
        </p>
        
        <div style="display: flex; flex-direction: column; gap: 1rem; align-items: center; margin-bottom: 2rem;">
            <button class="btn btn--primary" data-cta style="min-width: 250px; padding: 1.2rem 2rem; font-size: 1.2rem;">
                {{ user.language.code == 'ar' ? 'ابدأ تحولك الآن' : 'Start Your Transformation Now' }}
            </button>
            <div style="display: flex; align-items: center; gap: 0.5rem; color: var(--color-text-muted); font-size: 0.9rem;">
                <span style="color: var(--color-primary);">⏰</span>
                <span data-countdown>{{ user.language.code == 'ar' ? 'العرض ينتهي خلال 48 ساعة' : 'Offer ends in 48 hours' }}</span>
            </div>
        </div>
        
        <div style="display: flex; justify-content: center; gap: 2rem; flex-wrap: wrap;">
            <div style="display: flex; align-items: center; gap: 0.5rem; color: var(--color-text-muted);">
                <span style="color: var(--color-primary);">✓</span>
                {{ user.language.code == 'ar' ? 'بدون مخاطر' : 'Risk-free' }}
            </div>
            <div style="display: flex; align-items: center; gap: 0.5rem; color: var(--color-text-muted);">
                <span style="color: var(--color-primary);">✓</span>
                {{ user.language.code == 'ar' ? 'إلغاء في أي وقت' : 'Cancel anytime' }}
            </div>
            <div style="display: flex; align-items: center; gap: 0.5rem; color: var(--color-text-muted);">
                <span style="color: var(--color-primary);">✓</span>
                {{ user.language.code == 'ar' ? 'دعم 24/7' : '24/7 Support' }}
            </div>
        </div>
    </div>
</section>

{% endblock %}

{% block scripts %}
<script>
// Multi-step form functionality
document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('.multi-step-form');
    if (!form) return;
    
    const steps = form.querySelectorAll('.form-step');
    const stepIndicators = form.querySelectorAll('.step');
    const progressFill = form.querySelector('.progress-fill');
    const totalSteps = steps.length;
    let currentStep = 0;
    
    function showStep(stepIndex) {
        steps.forEach((step, index) => {
            step.classList.toggle('active', index === stepIndex);
        });
        
        stepIndicators.forEach((indicator, index) => {
            indicator.classList.toggle('active', index === stepIndex);
            indicator.classList.toggle('completed', index < stepIndex);
        });
        
        const progress = ((stepIndex + 1) / totalSteps) * 100;
        progressFill.style.width = progress + '%';
        
        // Scroll to top of form
        form.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }
    
    // Next step buttons
    form.querySelectorAll('.next-step').forEach(button => {
        button.addEventListener('click', () => {
            const currentStepElement = steps[currentStep];
            const requiredFields = currentStepElement.querySelectorAll('[required]');
            let isValid = true;
            
            requiredFields.forEach(field => {
                if (!field.value || (field.type === 'radio' && !form.querySelector(`input[name="${field.name}"]:checked`))) {
                    isValid = false;
                }
            });
            
            if (isValid && currentStep < totalSteps - 1) {
                currentStep++;
                showStep(currentStep);
            }
        });
    });
    
    // Previous step buttons
    form.querySelectorAll('.prev-step').forEach(button => {
        button.addEventListener('click', () => {
            if (currentStep > 0) {
                currentStep--;
                showStep(currentStep);
            }
        });
    });
    
    // Form submission
    form.addEventListener('submit', (e) => {
        e.preventDefault();
        
        // Collect form data
        const formData = new FormData(form);
        const data = Object.fromEntries(formData);
        
        // Track form submission
        if (window.FunnelAnalytics) {
            window.FunnelAnalytics.trackEvent('form_submit', {
                formType: 'pre-qualification',
                data: data
            });
        }
        
        // Show success message or redirect
        alert('{{ user.language.code == "ar" ? "شكراً لك! سنقوم بالتواصل معك قريباً." : "Thank you! We will contact you soon." }');
        
        // Reset form
        form.reset();
        currentStep = 0;
        showStep(currentStep);
    });
});

// FAQ functionality
document.addEventListener('DOMContentLoaded', function() {
    const faqQuestions = document.querySelectorAll('.faq-question');
    
    faqQuestions.forEach(question => {
        question.addEventListener('click', () => {
            const faqItem = question.closest('.faq-item');
            const answer = faqItem.querySelector('.faq-answer');
            const isActive = question.classList.contains('active');
            
            // Close all other FAQs
            document.querySelectorAll('.faq-question').forEach(q => {
                q.classList.remove('active');
                q.closest('.faq-item').querySelector('.faq-answer').classList.remove('open');
            });
            
            // Toggle current FAQ
            if (!isActive) {
                question.classList.add('active');
                answer.classList.add('open');
            }
        });
    });
});
</script>
{% endblock %}
